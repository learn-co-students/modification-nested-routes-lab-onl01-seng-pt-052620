<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= artist_url(@song, @song.artist) %><br>
   <%= f.select :artist_id, options_from_collection_for_select(Artist.all, :id, :name) %><br>
  <br />

  <%= f.submit %>
<% end %>